{{!-- This is the navigation bar partial. --}}
<nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="container-fluid">
        <div class="d-flex">
            <div class="p-2">
                <a class="navbar-brand" href="/">
                    <img class="logo img-fluid" src="/images/cameraShare_logo.png" alt="logo"
                        style="width: 200px; height: 200px;">
                </a>
            </div>
            <div class="navbar-brand d-flex justify-content-center">
                <h1>Share camGear</h1>
            </div>
        </div>
        <div class="d-flex">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/users">My Profile</a>
                </li>
                {{#if loggedIn}}
                <li class="nav-item">
                    <button id="logout" class="btn btn-danger" id="logout">Logout</button>
                </li>
                {{else}}
                <li class="nav-item">
                    <a id="login" class="btn btn-primary" href="/users/login">Login</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>