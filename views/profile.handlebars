{{!-- <div class="test">
    <p>Username:{{ user.username }}</p>
    <p>Email: {{ user.email }}</p>
    <H1>Gear</H1>
    {{#each user.gears as |gear|}}
    <p>Product name: {{ gear.product_name }}</p>
    <p>Category: {{ gear.category }}</p>
    <p>Is it available: {{ gear.available }}</p>
    {{/each}}
    <h1>items lent</h1>
    {{#each user.items_lent as |loan|}}
    <p>Product id:{{ loan.product_id }}</p>
    <p>Loan Status:{{ loan.status }}</p>
    {{/each}}
    <h1>items borrowed</h1>
    {{#each user.items_borrowed as |loan|}}
    <p>Product id:{{ loan.product_id }}</p>
    <p>Loan Status:{{ loan.status }}</p>
    {{/each}}
    <p>Logged in: {{ loggedIn }}</p>
</div> --}}

<div class="container my-4 mx-auto">
    <!-- GREETING SECTION-->
    <div>
        <h2>Welcome, {{ user.username }}</h2>
    </div>

    <!-- REQUEST NOTIFICATION SECTION-->
    <div class="view-request">
        <p>You've got *#* new borrow request(s)</p>
        <button id="view-request" type="submit">View Request</button>
    </div>


<!-- REVIEW REQUESTS SECTION-->
<div class="request" style="display: none;">
    <!-- REQUEST CARD SECTION-->
    {{#each user.items_lent as |lent|}}
    {{#if (check_requested lent.status)}}
    {{> request }}
    {{/if}}
    {{/each}}
</div>


    <!-- MY LISTINGS SECTION-->
    <div>

        <a href="/gear/add">Add New Listing</a>
    </div>
    <div>
        <!-- LISTING CARD -->
        {{#each user.gears as |listing|}}
        {{> profile-listing }}
        {{/each}}
    </div>

</div>


    <!-- ITEMS RENTED SECTION-->
    <div>
        <div>
            {{#each user.gears as |rented|}}
            {{> profile-rented}}
            {{/each}}

        </div>
    </div>
</div>